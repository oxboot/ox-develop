dist: xenial
sudo: true
language: php
php:
  - 5.6
  - 7.1
services:
  - docker
before_install:
  - docker pull ubuntu
  - docker run -it ubuntu
before_script:
script:
  - docker exec -t ubuntu sudo ./install.sh
